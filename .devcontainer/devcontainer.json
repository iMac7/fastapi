{
  "name": "fastapi_app",
  "service": "app",
  "dockerComposeFile": "../docker-compose.yml",
  "workspaceFolder": "/app",
  // "postCreateCommand": "uvicorn --reload main:app --host 0.0.0.0 --port 8000",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}

